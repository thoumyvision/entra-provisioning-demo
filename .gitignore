# Secrets / local config
*.env
.env
secrets.*
*.key
*.pfx
*.pem

# Outputs / per-machine
outputs/
*.log
# NOTE: data/new-hires.csv is a tracked demo artifact, so *.csv is intentionally NOT ignored here.

# OS / editor cruft
Thumbs.db
.DS_Store
.vscode/
